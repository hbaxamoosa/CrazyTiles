buildscript {
    ext{
        // sdk and tools
        compileSdkVersion = 31
        minSdkVersion = 25
        targetSdkVersion = 31

        // app dependencies
        activityVersion = '1.4.0'
        appCompatVersion = '1.4.0'
        constraintLayoutVersion = '2.1.2'
        coreVersion = '1.7.0'
        coroutinesVersion = '1.3.9'
        firebaseAnalyticsVersion = '20.0.0'
        firebaseCrashlyticsVersion = '18.2.4'
        firebaseCrashlyticsGradleVersion = '2.5.2'
        firebasePerformanceVersion = '20.0.3'
        firebaseRemoteConfigVersion = '21.0.1'
        fragmentVersion = '1.4.0'
        gradleVersion = '4.2.1'
        kotlinVersion = '1.6.0'
        lifecyclesVersion = '2.4.0'
        materialDesignVersion = '1.4.0'
        navigationVersion = '2.4.0-beta02'
        playServicesVersion = '4.3.3'
        preferenceVersion = '1.1.1'
        roomVersion = '2.4.0-beta02'
        timberVersion = '5.0.1'
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.0.3'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinVersion"
        classpath "androidx.navigation:navigation-safe-args-gradle-plugin:$navigationVersion"
        classpath "com.google.gms:google-services:$playServicesVersion"
        classpath "com.google.firebase:firebase-crashlytics-gradle:$firebaseCrashlyticsGradleVersion"
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
